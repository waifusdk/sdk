{
  "name": "@waifusdk/sdk",
  "license": "MIT",
  "version": "0.0.2",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "release": "pnpm run build && changeset publish",
    "lint": "tsc"
  },
  "dependencies": {
    "@waifusdk/providers": "^0.0.6"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
